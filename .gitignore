# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
# Bridge secrets and workshop data: dải mạng nội bộ, token, sổ máy, audit log.
.env
.env.*
!.env.example
bridge.config.json
bridge.config.*.json
!bridge.config.example.json
bridge-data/
# Cấu hình node cảm biến: chứa mật khẩu WiFi xưởng. Chỉ bản .example.h được commit.
firmware/*/config.h

# bản ghi byte thô bắt từ máy thêu — dữ liệu hiện trường, không đưa vào repo
bat-goi-*.bin
bat-goi-*.jsonl

# Báo cáo độ phủ: sinh lại bằng `npm run coverage`
coverage

# Số đo T_chuẩn bị do broker tự nối thêm mỗi lần nạp mẫu — là kết quả đo, không phải mã.
deploy-mini/patterns-nap.csv

# Số đo nhịp `state` (S‑11 min/max, K‑20c tỉ lệ dòng lặp) — broker tự nối thêm 5 phút một dòng.
deploy-mini/nhip-state.csv
