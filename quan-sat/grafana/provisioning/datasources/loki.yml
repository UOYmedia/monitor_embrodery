apiVersion: 1

datasources:
  - name: Loki
    uid: loki-dahao
    type: loki
    access: proxy
    # Tên dịch vụ trong mạng nội bộ của compose, không đi qua cổng máy chủ.
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 2000
      timeout: 60
